# DevOps Assignment 

As per the assignment tasks, I have successfully implemented a end-to-end CI CD pipeline, that provisions the infra (EKS) and deploys the application on the EKS cluster based on eact requirements. 

Additionally I have implemented the security checks that conducts security checks and identifies misconfigurations in Docker Image, Terraform and Helm Charts. More detail on design decision will be explored on the call. 

Also I have implemented the Application LoadBalancer 